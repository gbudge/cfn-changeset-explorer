AWSTemplateFormatVersion: '2010-09-09'
Description: Policies nested stack — attaches inline and managed policies to a role

Parameters:
  RoleName:
    Type: String
    Description: Name of the IAM role to attach policies to

Resources:
  MyRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub "${AWS::StackName}-${RoleName}"
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - ec2.amazonaws.com
                - lambda.amazonaws.com
            Action:
              - sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole

Outputs:
  RoleName:
    Description: Name of the created IAM role
    Value: !Ref MyRole